[pytest]
# backend/ is not an installed package — main.py adds itself to sys.path at
# import time, and the tests need the same thing before any import runs.
pythonpath = backend
testpaths = backend/tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
